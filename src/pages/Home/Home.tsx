import React from "react";
import { Modal } from "../../components/Modal/Modal";

type Props = {};

export const Home = (props: Props) => {
  return (
    <div className="home">
      <Modal />
    </div>
  );
};
